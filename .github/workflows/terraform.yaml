name: Terraform
run-name: "Terraform | ${{ github.event_name }} | ${{ github.ref_name }}"

concurrency:
  group: terraform-${{ github.ref }}
  cancel-in-progress: false

on:
  pull_request:
    paths:
      - "terraform/**"
      - ".github/workflows/terraform.yaml"

  push:
    branches:
      - main
    paths:
      - "terraform/**"
      - ".github/workflows/terraform.yaml"

  workflow_dispatch:

permissions:
  contents: read

jobs:

  plan:
    runs-on: ubuntu-latest
    environment: staging
    if: github.event_name == 'pull_request' || github.event_name == 'push' || github.event_name == 'workflow_dispatch'
    env:
      AWS_DEFAULT_REGION: eu-north-1
      TF_INPUT: "false"
      TF_STATE_BUCKET: ${{ secrets.TF_STATE_BUCKET }}
      TF_STATE_KEY: hospital-backend/terraform.tfstate

    steps:
      - uses: actions/checkout@v4

      - uses: hashicorp/setup-terraform@v3
      
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_DEFAULT_REGION }}

      - name: Verify AWS credentials
        run: aws sts get-caller-identity

      - name: Validate backend config
        run: |
          test -n "${TF_STATE_BUCKET}" || (echo "TF_STATE_BUCKET secret is empty" && exit 1)

      - run: |
          terraform init -input=false -reconfigure \
            -backend-config="bucket=${TF_STATE_BUCKET}" \
            -backend-config="key=${TF_STATE_KEY}" \
            -backend-config="region=${AWS_DEFAULT_REGION}" \
            -backend-config="use_lockfile=true" \
            -backend-config="encrypt=true"
        working-directory: terraform

      - name: Generate tfvars from secrets
        run: |
          cat > terraform.tfvars << EOF
          vpc_id              = "${{ secrets.VPC_ID }}"
          control_instance_id = "${{ secrets.CONTROL_INSTANCE_ID }}"
          worker_instance_id  = "${{ secrets.WORKER1_INSTANCE_ID }}"
          worker2_instance_id = "${{ secrets.WORKER2_INSTANCE_ID }}"
          worker3_instance_id = "${{ secrets.WORKER3_INSTANCE_ID }}"
          EOF
        working-directory: terraform

      - name: Reset stale security group state
        run: terraform state rm 'module.security' || true
        working-directory: terraform

      - run: terraform plan -input=false -no-color -var-file=terraform.tfvars
        working-directory: terraform


  apply:
    runs-on: ubuntu-latest
    needs: plan
    environment: staging
    if: (github.event_name == 'push' && github.ref == 'refs/heads/main' && needs.plan.result == 'success') || (github.event_name == 'workflow_dispatch' && needs.plan.result == 'success')
    env:
      AWS_DEFAULT_REGION: eu-north-1
      TF_INPUT: "false"
      TF_STATE_BUCKET: ${{ secrets.TF_STATE_BUCKET }}
      TF_STATE_KEY: hospital-backend/terraform.tfstate

    steps:
      - uses: actions/checkout@v4

      - uses: hashicorp/setup-terraform@v3
      
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_DEFAULT_REGION }}

      - name: Verify AWS credentials
        run: aws sts get-caller-identity

      - name: Validate backend config
        run: |
          test -n "${TF_STATE_BUCKET}" || (echo "TF_STATE_BUCKET secret is empty" && exit 1)

      - run: |
          terraform init -input=false -reconfigure \
            -backend-config="bucket=${TF_STATE_BUCKET}" \
            -backend-config="key=${TF_STATE_KEY}" \
            -backend-config="region=${AWS_DEFAULT_REGION}" \
            -backend-config="use_lockfile=true" \
            -backend-config="encrypt=true"
        working-directory: terraform

      - name: Generate tfvars from secrets
        run: |
          cat > terraform.tfvars << EOF
          vpc_id              = "${{ secrets.VPC_ID }}"
          control_instance_id = "${{ secrets.CONTROL_INSTANCE_ID }}"
          worker_instance_id  = "${{ secrets.WORKER1_INSTANCE_ID }}"
          worker2_instance_id = "${{ secrets.WORKER2_INSTANCE_ID }}"
          worker3_instance_id = "${{ secrets.WORKER3_INSTANCE_ID }}"
          EOF
        working-directory: terraform

      - name: Reset stale security group state
        run: terraform state rm 'module.security' || true
        working-directory: terraform

      - run: terraform plan -input=false -no-color -var-file=terraform.tfvars
        working-directory: terraform

      - run: terraform apply -input=false -auto-approve -var-file=terraform.tfvars
        working-directory: terraform

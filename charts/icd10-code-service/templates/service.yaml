apiVersion: v1
kind: Service
metadata:
  name: {{ include "icd10-code-service.fullname" . }}
  labels:
    {{- include "icd10-code-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "icd10-code-service.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
